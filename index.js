// Function expression called divide
const divide = function() {
  return 2000 / 100;
};

// Arrow function called square (takes one parameter)
const square = x => x * x;

// Arrow function called add (takes two parameters)
const add = (a, b) => a + b;
